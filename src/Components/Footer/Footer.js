import React from "react";
import { Layout} from 'antd';

const { Footer } = Layout;

const FooterBlock = () => {
  return (
      <Footer id='footer' style={{ textAlign: 'center',fontSize:'16px',fontWeight:'bold'}} >Created By Reactive Developerz.</Footer>
  )
  ;
};

export default FooterBlock;
